export default class SkapiError extends Error {
    code: string | number;
    cause: Error;
    constructor(error: any, options?: {
        name?: string;
        code?: string;
        cause?: Error;
    });
}
